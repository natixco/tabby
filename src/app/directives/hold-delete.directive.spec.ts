import { HoldDeleteDirective } from './hold-delete.directive';

describe('HoldDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new HoldDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
